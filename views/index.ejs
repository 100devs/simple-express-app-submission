<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>iStore Groceries</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/reset.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    
    <form action="/addgroceries" method="POST">
        <input type="text" placeholder="Add groceries" name="newgrocery">
        <input type="submit">
    </form>

    <h3>Your grocery list:</h3>
    <ul class="userList">
        <% for(let i = 0; i < items.length; i++) { %>
        <li class="listedItems">
            <% if(items[i].completed === true) { %>
            <span class="completed"><%= items[i].newItem %></span>
            <% } else { %>
            <span class="incomplete"><%= items[i].newItem %></span>
            <% } %>
            <span class='fa fa-trash'></span>
        </li>
        <% } %>
    </ul>


    <script src="js/main.js"></script>
</body>
</html>