//for coding along with the traversy Node JS crash course video on YT
//this file has nothing to do with BB quotes DB (delete when done)

const url = require('url')

const myUrl = new URL('http://mywebsite.com/hello.html')