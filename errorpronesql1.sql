INSERT INTO Posts (PostTitle, PostSubtitle, PostMainBody, PostConclusion, PostCommentCount) 
	VALUES ('Cannot GET / Nodejs Error','n/a', 'I\'m using the tutorial found here: http://addyosmani.github.io/backbone-fundamentals/#create-a-simple-web-server and added the following code.  // Module dependencies. var application_root = __dirname, express = require( \'express\' ), //Web framework path = require( \'path\' ), //Utilities for dealing with file paths mongoose = require( \'mongoose\' ); //MongoDB integration  //Create server var app = express();  // Configure server app.configure( function() { //parses request body and populates request.body app.use( express.bodyParser() );  //checks request.body for HTTP method overrides app.use( express.methodOverride() );  //perform route lookup based on url and HTTP method app.use( app.router );  //Where to serve static content app.use( express.static( path.join( application_root, \'site\') ) );  //Show all errors in development app.use( express.errorHandler({ dumpExceptions: true, showStack: true })); });  //Start server var port = 5000; app.listen( port, function() { console.log( \'Express server listening on port %d in %s mode\', port, app.settings.env ); }); After starting the server with node server.js I\'m getting an error stating Cannot GET / when 
I access localhost:5000 and I just wondered if anyone knew much about this error as Express and Node are new to me?'', ''I think you\'re missing your routes, you need to define at least one route for example \'/\' to index.  e.g.  app.get(\'/\', function (req, res) {   res.render(\'index\', {}); });'', 0)